{
  "name": "@decent-render/protocol",
  "version": "0.1.1",
  "description": "Decent render-network wire protocol (v2) — shared TS types and zod schemas, pinned to Rust by shared fixtures and cross-language conformance tests.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "fixtures",
    "CHANGELOG.md",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "zod": ">=4 <5"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "zod": "^4.3.6"
  },
  "keywords": [
    "decent-render",
    "render-farm",
    "protocol",
    "wire-protocol",
    "remotion"
  ]
}
