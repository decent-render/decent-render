{
  "name": "@decent-render/protocol",
  "version": "0.1.0",
  "description": "Decent render-network wire protocol (v2) — shared TS types + zod schemas. Canonical to the Rust supervisor-core (crates/supervisor-core/src/protocol.rs); this package is the TS mirror, pinned by a cross-language conformance test.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "fixtures",
    "CHANGELOG.md",
    "README.md"
  ],
  "scripts": {
    "test": "echo 'run via vitest from the consumer / repo root' && exit 0"
  },
  "peerDependencies": {
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "zod": "^3.23.8"
  },
  "keywords": [
    "decent-render",
    "render-farm",
    "protocol",
    "wire-protocol",
    "remotion"
  ]
}
